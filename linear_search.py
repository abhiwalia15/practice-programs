def search(key):
	count = 0
	n = int(input("ENTER n")
	for i in range(0,n+1)
		nums = int(input())
		if (i==nums):
			count = 1
	if count==1:
		print("KEY FOUND")
	else:
		print("KEY NOT FOUND")
	
search(4)
		
